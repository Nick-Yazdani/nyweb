import React from 'react'

import { Container, Title } from './Soon.elements'

export default function Soon({ title }) {
    return (
        <Container>
            <Title>{title}</Title>
        </Container>
    )
}
